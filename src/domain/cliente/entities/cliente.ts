export class Cliente {
  nome: string;
  sexo: string;
  idade: number;
  constructor(nome: string, sexo: string, idade: number) {
    this.nome = nome;
    this.sexo = sexo;
    this.idade = idade;
  }
}
